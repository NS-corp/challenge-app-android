<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="corp.ns.challengeapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="corp.ns.challenge_app_android.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="corp.ns.challenge_app_android.UserActivity">
        </activity>
        <activity android:name="corp.ns.challenge_app_android.HistoryActivity" />
        <activity android:name="corp.ns.challenge_app_android.ChallengeListActivity" />
        <activity android:name="corp.ns.challenge_app_android.ChallengeActivity" />
        <activity android:name="corp.ns.challenge_app_android.CreateChallengeActivity" />
        <activity android:name="corp.ns.challenge_app_android.TaskActivity" />
        <activity android:name="corp.ns.challenge_app_android.CreateTaskActivity" />
        <activity
            android:name="corp.ns.challenge_app_android.AuthActivity"
            android:label="@string/title_activity_auth">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>